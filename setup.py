from setuptools import setup

setup(
   name='window_overall',
   version='0.0.1',
   description='',
   author='S Zhang',
   author_email='szhan.me@gmail.com',
   packages=['window_overall'],  #same as name
   install_requires=['pywincalc'], #external packages as dependencies
)